# Artifacts from `python -m cover6 experiment`

This directory is populated by the experiment runner (JSON coverings, SVG/PNG figures, search logs). See the repository `RESULTS.md` for the interpretation.
